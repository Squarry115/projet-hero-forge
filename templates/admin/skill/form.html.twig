{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<h1>{{ title }}</h1>
{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.ability) }}
    {{ form_row(form.id_class) }}
    <button type="submit">Sauvegarder</button>
{{ form_end(form) }}
<a href="{{ path('admin_skill_index') }}">← Retour</a>
{% endblock %}
